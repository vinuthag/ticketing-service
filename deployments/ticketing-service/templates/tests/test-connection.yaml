apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ticketing-service.fullname" . }}-test-connection"
  labels:
    {{- include "ticketing-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ticketing-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
