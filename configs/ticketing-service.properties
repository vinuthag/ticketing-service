HOST:localhost
DB_PORT:5432
DB_USER:postgres
DB_NAME:ticketing_service
TICKETING_SYSTEM_DB_TABLE:reservation
IS_POSTGRES_ENABLED:true
DB_CONNECTION_TIMEOUT_SECOND:1
#SQLITE_MOUNT_PATH:/var/db
CREATE_TABLE_QUERY:CREATE TABLE if not exists reservation(ticket_id int GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,id VARCHAR (34) NOT NULL,name VARCHAR (120) NOT NULL,place_to VARCHAR (34) NOT NULL,place_from VARCHAR (34) NOT NULL,date VARCHAR (34) NOT NULL,time VARCHAR (34) NOT NULL);

TICKETING_SERVICE_PORT:8080

RMQ_EVENT_EXCHANGE:ticketing.service
RMQ_HOST:localhost
RMQ_PORT:31302
RMQ_USER:admin
RMQ_RETRY_DELAY_MILLISEC:50
RMQ_RETRY_MAX_ATTEMPTS:3
RMQ_CONNECTION_TIME_OUT_MS:50
RMQ_VHOST:/
IS_RMQ_ENABLED:true
LOG_LEVEL:debug
